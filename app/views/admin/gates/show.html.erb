<div>
  <h1>Gate: <%= @gate.title %></h1>
  <p>공지 내용 : <%= @gate.content %></p>
  <p>실제 공지 주소 : <%= @gate.link %></p>
  <p>통합 공지 주소 : <%= @gate.shortenURL %></p>
  <p>만든 시간 : <%= @gate.created_at.localtime.strftime("%m/%d/%Y %T%:z") %></p>
  
  <h3>Read Users ( <%= @gate.read_users.count %> )</h3>
    <div class="table-responsive">
    <table class="table table-condensed">
    <thead>
      <tr>
        <th>이름</th>
        <th>전화번호</th>
        <th>이메일</th>
        <th>전공</th>
      </tr>
    </thead>
    <tbody>
      <% @gate.read_users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.phonenumber %></td>
        <td><%= user.email %></td>
        <td><%= user.major %></td>
      </tr>
      <% end %>
    </tbody>
    </table>
    </div>

    <h3>Unread Users ( <%= @gate.unread_users.count %> )</h3>
    <div class="table-responsive">
    <table class="table table-condensed">
    <thead>
      <tr>
        <th>이름</th>
        <th>전화번호</th>
        <th>이메일</th>
        <th>전공</th>
      </tr>
    </thead>
    <tbody>
      <% @gate.unread_users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
        <td><%= user.phonenumber %></td>
        <td><%= user.email %></td>
        <td><%= user.major %></td>
      </tr>
      <% end %>
    </tbody>
    </table>
    </div>
</div>