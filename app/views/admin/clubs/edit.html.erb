  <section class="portfolio-item-details">
    <div class="container">
      <div class="row item-title">
        <div class="col-sm-9">
          <h1>클럽 정보 수정하기</h1>
          <div class="categories">
            <a></a>
          </div>
        </div>

        <div class="col-sm-3">
          <div class="text-right">
            <div class="item-detail">
            </div>
            <div class="item-detail">
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="portfolio-container">
    <div class="container">
      <div class="panel panel-primary">
        <div class="panel-body">
          <%= form_for @club, url: club_admin_edit_club_path(current_club), html: {role: "form", class: "form-horizontal"} do |f| %>
            <%= render partial: 'form', locals: {f: f} %>
            <div class="pull-right">
              <%= f.button type: :submit, class: "btn btn-info btn-lg btn-icon" do %>
                수정<i class="entypo-check"></i>
              <% end %>
              <%= link_to club_admin_root_path(current_club) do %>
                <button type="button" class="btn btn-gold btn-lg btn-icon">
                  취소<i class="entypo-cancel"></i>
                </button>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </section>